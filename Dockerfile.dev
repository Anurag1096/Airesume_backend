# Dockerfile.dev
FROM node:20-alpine
WORKDIR /app
RUN corepack enable && corepack prepare pnpm@latest --activate
COPY package.json pnpm-lock.yaml ./
RUN pnpm install # Installs EVERYTHING including ts-node-dev
COPY . .
CMD ["pnpm", "run", "dev"]
